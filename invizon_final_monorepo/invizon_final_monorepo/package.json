{
  "name": "invizon_final_monorepo",
  "version": "1.0.0",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build && cp -r dist ../backend/public || true",
    "start": "npm run build && cd backend && node server.js"
  }
}